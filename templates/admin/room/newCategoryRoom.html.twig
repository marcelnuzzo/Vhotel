{% extends 'admin/base.html.twig' %}

{% block title %}Nouvel catégorie de chambre{% endblock %}

{% block body %}

{% if editMode %}
    <div class="jumbotron" style="background-color:orange">
        <h1>Modification de la catégorie de la chambre</h1>
    </div>                
{% else %}
    <div class="jumbotron" style="background-color:cyan">
        <h1>Création d'une catégorie de chambre</h1>
    </div>
{% endif %}

<div class="container">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-6">
        {{ form_start(form) }}
        {{ form_row(form.label, {'attr': {'placeholder': "Nom de la catégorie de la chambre"}}) }}
            <button type="submit" class="btn btn-success">
                {% if editMode %}
                    Modifier
                {% else %}
                    Créer
                {% endif %}
            </button>
        {{ form_end(form) }}
        </div>
        <div class="col-3"></div>
    </div>
</div>
{% endblock %}