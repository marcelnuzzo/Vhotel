{% extends 'admin/base.html.twig' %}

{% block title %}Liste des utilisateurs pour les roles!{% endblock %}

{% block body %}

<div class="container-fluid my-3">
    <div class = "jumbotron">
        <h1 style="text-align:center"><mark>Les utilisateurs</mark></h1> 
        <a href="{{ path('homepage') }}" class="btn btn-info">Retour au menu principale</a> 
    </div>
    <h2><mark>{{ user | length }} utilisateurs</mark></h2>
    
    <table class="table table-striped table-bordered table-hover">
        <tr>
            <th>Prénom</th>
            <th>Nom</th>
            <th>Email</th>
            <th>Photo</th>
            <th>Role</th>
        </tr>
        {%for user in users %} 
            <tr>
                <td>{{ user.firstname }}</td>
                <td>{{ user.lastname }}</td>
                <td>{{ user.email }}</td>
                <td>
                    <img src="{{ user.picture }}" class="avatar avatar-medium" alt="Avatar de {{user.firstname}} {{user.lastname}}">
                </td>
                <td>
                    <ul>
                    {% for role in user.roles %}
                            <li>{{ role }}</li>
                    {% endfor %}
                    </ul>
                </td>
                <td><a class="btn btn-info" style="color:white" href="{{ path("admin_admin_assignRole", {'id':user.id}) }}" role="button" >Modifier</a></td>
            </tr>
        {% endfor %}
    </table>
</div>
  
{% endblock %}